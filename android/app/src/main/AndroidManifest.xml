<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:screenOrientation="portrait"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              TWA Asset Links — the TWA launches https://eduvora.shop/ inside
              the WebView only after Google verifies this association via the
              /.well-known/assetlinks.json file served from the origin (set up
              during Play Console submission). Without this, the URL bar shows
              the address and a "Open in browser" banner; with it, the app
              runs fullscreen and looks like a native app.
            -->
            <meta-data
                android:name="android.support.customtabs.trustedurl"
                android:resource="@array/trusted_web_activities" />
            <meta-data
                android:name="asset_statements"
                android:resource="@string/asset_statements" />

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
      Permissions required for guaranteed-delivery notifications + FCM:

      • INTERNET                       — every API call + FCM transport
      • POST_NOTIFICATIONS             — Android 13+ runtime permission
                                         for the LocalNotifications plugin
                                         (this is the one the user will
                                         see a system prompt for on first
                                         install)
      • SCHEDULE_EXACT_ALARM           — exact-time local alarms
      • USE_EXACT_ALARM                — Android 14+ replacement for
                                         SCHEDULE_EXACT_ALARM that doesn't
                                         require a user-granted permission
                                         (system alarms like ours qualify
                                         for this category)
      • RECEIVE_BOOT_COMPLETED          — re-schedule local alarms after
                                         a device reboot
      • WAKE_LOCK                      — let the AlarmManager fire even
                                         when the screen is off
      • VIBRATE                        — system notification vibration
      • FOREGROUND_SERVICE             — keeps the app responsive while a
                                         notification handler is running

      No push permission is needed for FCM itself — Google's services
      handle delivery via Play Services. The above are what we add on
      top of FCM so we can also render the local alarm at the exact
      wall-clock time.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- The FCM service is provided by Google Play Services — no extra
         <service> entry is needed in our manifest. -->
</manifest>
