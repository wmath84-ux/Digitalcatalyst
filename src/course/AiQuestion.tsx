import { Bot, Send, Sparkles } from "lucide-react";

export default function AiQuestion({ draft, setDraft, fileName, onOpen }: { draft: string; setDraft: (value: string) => void; fileName?: string; onOpen: () => void }) {
  return <div className="flex h-full flex-col overflow-y-auto p-4"><div className="rounded-2xl bg-gradient-to-br from-violet-500/20 to-cyan-400/10 p-4 ring-1 ring-violet-400/20"><div className="grid h-11 w-11 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-cyan-400 text-white"><Bot size={22} /></div><h3 className="mt-4 text-base font-black">Ask the Community AI</h3><p className="mt-1 text-xs leading-5 text-white/45">This opens the same AI workspace used by the Community, with your current course and resource context attached.</p>{fileName && <div className="mt-3 rounded-xl bg-white/5 px-3 py-2 text-[11px] font-bold text-cyan-200"><Sparkles className="mr-1 inline h-3 w-3" /> Context: {fileName}</div>}</div><textarea value={draft} onChange={(event) => setDraft(event.target.value)} rows={5} placeholder="Ask a question about this lesson or resource…" className="mt-4 w-full rounded-xl border border-white/10 bg-white/5 p-3 text-sm text-white outline-none placeholder:text-white/25 focus:border-violet-400" /><button onClick={onOpen} className="mt-3 flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-violet-500 to-cyan-500 py-3 text-xs font-black text-white"><Send size={15} /> Open AI & ask</button><p className="mt-3 text-center text-[10px] text-white/30">Your question will be pre-filled in the shared AI chat.</p></div>;
}
