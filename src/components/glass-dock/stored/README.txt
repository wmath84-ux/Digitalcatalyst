Original footer / photo-dock markup, stored before the GlassDock UX swap.

These files are the previous white-pill implementations. Navigation logic
was copied forward into the live BottomNav / ImageViewer files; only the
visual dock was replaced.

  BottomNav.original.txt            — main app footer
  RevisionBottomNav.original.txt    — revision footer
  MyDayBottomNav.original.txt       — My Day footer
  CartBottomNav.original.txt        — cart / wishlist footer
  ImageViewer.original.txt          — course image zoom dock
