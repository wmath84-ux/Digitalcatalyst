# One-shot: download the exact mobile EduOS opening animation from GitHub
# user-attachments (S3 is unreachable from the Arena sandbox) and commit
# it to this branch. Also remove the previous (non-mobile) MP4.
name: Fetch EduOS opening animation

on:
  push:
    branches: [arena/01a06cef-digitalcatalyst]
    paths:
      - ".github/workflows/fetch-eduos-opening.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: arena/01a06cef-digitalcatalyst
          fetch-depth: 1

      - name: Download the exact mobile opening animation
        run: |
          set -euo pipefail
          mkdir -p public/assets/animations
          dest="public/assets/animations/EduOS_app_opening_mobile.mp4"
          curl -L --fail --retry 8 --retry-delay 2 --retry-all-errors \
            -A "Mozilla/5.0" \
            -o "$dest" \
            "https://github.com/user-attachments/assets/540389f9-1306-4e00-9b83-ebede2565bf3"
          ls -lh "$dest"
          file "$dest"
          python3 - <<'PY'
          from pathlib import Path
          p = Path("public/assets/animations/EduOS_app_opening_mobile.mp4")
          data = p.read_bytes()
          assert len(data) > 10_000, f"file too small: {len(data)}"
          assert data[4:8] == b"ftyp", f"not an mp4 ftyp box: {data[:16]!r}"
          print(f"ok bytes={len(data)}")
          PY
          rm -f public/assets/animations/EduOS_app_opening.mp4

      - name: Commit the mobile video onto this branch
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A public/assets/animations
          if git diff --cached --quiet; then
            echo "Video already present; nothing to commit."
            exit 0
          fi
          git commit -m "Replace opening animation with exact mobile EduOS MP4"
          git push origin arena/01a06cef-digitalcatalyst
