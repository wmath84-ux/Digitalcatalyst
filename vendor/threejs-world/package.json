{
  "name": "laas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "threejs-world — procedural open world in the browser (WebGPU), built on LAAS",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 5174 --strictPort",
    "typecheck": "tsc --noEmit",
    "shoot": "tsx tools/shoot.ts",
    "compare": "tsx tools/compare.ts",
    "battery": "tsx tools/battery.ts"
  },
  "dependencies": {
    "three": "0.184.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/three": "^0.184.1",
    "@webgpu/types": "^0.1.60",
    "playwright": "^1.50.0",
    "sharp": "^0.34.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
